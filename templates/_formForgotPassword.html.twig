{% extends 'base.html.twig' %}
    {% block css%}
    <link rel="stylesheet" href="{{asset('assets/css/login.css')}}" />
    <link rel="stylesheet" href="{{asset('assets/css/avocado.css')}}" />
    {% endblock %}

    {% block body %}
    <img class="img" src="{{asset('assets/img/core-img/Avocado.png')}}" alt="Avocado Logo">

    <div class="content">
      <div class="flex-div">
        <div class="name-content">
          <h1 class="logo">Avocado</h1>
          <p>Get your best healthy recipes with us .</p>
        </div>

{{ form_start(form, {'attr': {'novalidate': 'novalidate'}, 'action': path('send_email'), 'method': 'post'}) }}

{{ form_row(form.email, {'attr': {'class': 'input', 'placeholder': 'Enter your email'}, 'label': false}) }}

<hr>

<button class="btn login" type="submit">Submit Email</button>

{{ form_end(form) }}

 </div>
    </div>
  {% endblock %}