{% extends 'base.html.twig' %}

{% block css %}
<!-- Bootstrap CSS -->
<link rel="stylesheet" href="{{ asset('assets/vendor/bootstrap/css/bootstrap.min.css') }}">
<link href="{{ asset('assets/vendor/fonts/circular-std/style.css') }}" rel="stylesheet">
<link rel="stylesheet" href="{{ asset('assets/libs/css/style.css') }}">
<link rel="stylesheet" href="{{ asset('assets/vendor/fonts/fontawesome/css/fontawesome-all.css') }}">
<style>
    html,
    body {
        height: 100%;
    }

    body {
        display: -ms-flexbox;
        display: flex;
        -ms-flex-align: center;
        align-items: center;
        padding-top: 40px;
        padding-bottom: 40px;
    }
</style>
{% endblock %}

{% block body %}
<form class="splash-container" method="post" enctype="multipart/form-data">
    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    <div class="card">
        <div class="card-header">
            <h3 class="mb-1">Product Details</h3>
        </div>
        <div class="card-body">
            <div class="form-group">
                {{ form_row(form.name, {'attr': {'class': 'form-control form-control-lg'}}) }}
            </div>
            <div class="form-group">
                {{ form_row(form.price, {'attr': {'class': 'form-control form-control-lg'}}) }}
            </div>
            <div class="form-group">
                {{ form_row(form.datefabrication, {'attr': {'class': 'form-control form-control-lg'}}) }}
            </div>
            <div class="form-group">
                {{ form_row(form.quantite, {'attr': {'class': 'form-control form-control-lg'}}) }}
            </div>
            <div class="form-group">
                {{ form_row(form.likes, {'attr': {'class': 'form-control form-control-lg', 'readonly': 'readonly'}}) }}
            </div>
            <div class="form-group">
                {{ form_row(form.imageFile, {'attr': {'class': 'form-control form-control-lg'}}) }}
            </div>
            <div class="form-group">
                {{ form_row(form.Category, {'attr': {'class': 'form-control form-control-lg ','readonly': 'readonly'}}) }}
                
            </div>
            <div class="form-group pt-2">
                <button type="submit" class="btn btn-block btn-primary">Save changes</button>
            </div>
        </div>
    </div>
    {{ form_end(form) }}
</form>
{% endblock %}